pipeline {
    agent {
        label 'image-worker'
    }
    stages {
        stage('Build') {
            steps {
                echo 'Starting Build'
            }
        }
        stage('Publish') {
            steps {
                echo 'Starting Publish'
            }
        }
    }
}
